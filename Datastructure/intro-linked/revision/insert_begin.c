#include <stdio.h>

#define kelly main

/*define node*/

struct Node
{
    int data;
    struct Node *next;
};


int kelly(void)
{
    /*define head pointer*/

    struct Node *head = NULL;

    /*create a node*/

    struct Node *node1 = (struct Node *)malloc()
}